spring:
  application:
    name: AssessMint BE

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  datasource:
    url: jdbc:postgresql://localhost:3332/db_pg_geb_cap?currentSchema=assessmint
    username: usr_pg_geb_cap
    password: pass_pg_geb_cap
    driver-class-name: org.postgresql.Driver

  mail:
    host: localhost
    port: 3125

server:
  port: 3380

app:
  frontend:
    url: http://localhost:8080
  gemini:
    model: gemini-2.0-flash
    api-key: AIzaSyBGFrKQt67qyUAgf5_3mfctiq19IQDFlRk

springdoc:
  api-docs:
    path: /api-doc
  swagger-ui:
    path: /api-doc.html
